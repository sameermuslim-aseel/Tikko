# Tikko — پلان فازهای بعدی (بعد از MVP)

> پیش‌فرض: MVP کامل است (ورود ایمیلی، household، نمای امروز، تسک ادمین/شخصی، کتگوری، اولویت، PWA).
> این فایل را کنار `PLAN.md` بگذارید و در `CLAUDE.md` اضافه کنید: «فازهای بعدی در PLAN-PHASE2.md».
> هر فاز را کامل بسازید، **یک تا دو هفته واقعاً استفاده کنید**، بعد فاز بعد را شروع کنید. چیزی که در استفادهٔ واقعی لازم نشد، حذف کنید.

---

## اصل راهنما

Tikko یک اپ دو نفره برای زندگی روزمره است، نه Jira. هر ویژگی باید به یکی از این سه سؤال جواب بدهد:
1. **آیا باعث می‌شود تسک فراموش نشود؟** (یادآوری، عقب‌افتاده‌ها)
2. **آیا باعث می‌شود بین دو نفر سوءتفاهم کم شود؟** (کی چی کرد، کی مسئول است)
3. **آیا اضافه کردن/انجام دادن تسک را سریع‌تر می‌کند؟** (کمتر از ۵ ثانیه)

اگر جواب هر سه «نه» است، نسازید.

---

## فاز ۲ — «واقعاً هر روز استفاده شود» (۲–۳ هفته)

مشکل MVP: اپ فقط وقتی کار می‌کند که خودتان یادتان بیاید بازش کنید. این فاز اپ را به سمت شما می‌آورد.

### ۲.۱ یادآوری و نوتیفیکیشن پوش
- Web Push (VAPID) با Supabase Edge Function + cron (`pg_cron`) که هر ۵ دقیقه تسک‌های `remind_at` رسیده را می‌فرستد.
- در iOS فقط وقتی اپ به Home Screen اضافه شده کار می‌کند (iOS 16.4+) — در onboarding این را با یک راهنمای دو مرحله‌ای بگویید.
- هر تسک: ساعت یادآوری اختیاری. هر کاربر: **ساعت خلاصهٔ صبح** (مثلاً ۷:۳۰: «امروز ۴ تسک داری») و **ساعات سکوت**.
- **یادآوری ملایم شب**: اگر تسک high تیک نخورده و ساعت ۲۱ شده → یک نوتیفیکیشن.

```sql
alter table tasks add column remind_at time;
alter table profiles add column morning_digest_time time default '07:30',
  quiet_from time default '22:00', quiet_to time default '07:00';
create table push_subscriptions (
  id uuid pk, profile_id uuid references profiles,
  endpoint text unique, p256dh text, auth text, created_at timestamptz
);
```

### ۲.۲ تسک‌های عقب‌افتاده (مهم‌ترین ویژگی این فاز)
- در بالای «امروز» یک بخش جمع‌شونده: **«از دیروز مانده (۲)»** با تسک‌های `once` انجام‌نشده و تسک‌های تکراری high دیروز.
- سه عمل روی هر مورد: **انجام شد** (تیک با تاریخ دیروز)، **امروز انجام می‌دهم** (کپی به امروز)، **رد شد** (skip با دلیل اختیاری).
- تسک‌های تکراری low/medium عقب‌افتاده نمایش داده نشوند — شلوغی می‌کند و حس عذاب وجدان می‌دهد.

```sql
alter table task_completions add column status text
  check (status in ('done','skipped')) default 'done',
  add column note text;
```

### ۲.۳ تسک مشترک («هر کی زودتر»)
- خیلی از کارهای خانه مال یک نفر مشخص نیست: نان گرفتن، بیرون گذاشتن زباله.
- `assigned_to` → nullable + `assignment_type in ('one','shared')`. تسک مشترک برای هر دو نمایش داده می‌شود؛ هر کس تیک زد، برای هر دو تیک می‌خورد و اسم او روی تسک می‌آید.

### ۲.۴ لیست خرید (نوع تسک `list`)
- کاربردی‌ترین چیز برای یک زوج. یک تسک با آیتم‌های داخلی (`task_items`), ریل‌تایم با Supabase Realtime تا وقتی یکی در فروشگاه است و دیگری آیتم اضافه می‌کند فوراً ببیند.
- آیتم‌های تیک‌خورده پایین می‌روند؛ «پاک کردن خریده‌شده‌ها» یک دکمه.
- لیست خرید همیشه در تب جدا یا بالای «امروز» پین شود.

```sql
create table task_items (
  id uuid pk, task_id uuid references tasks on delete cascade,
  title text, quantity text, is_done boolean default false,
  done_by uuid, sort_order int, created_at timestamptz
);
```

### ۲.۵ اضافه کردن سریع
- FAB → فقط یک فیلد متن، بقیه پیش‌فرض (کتگوری «عمومی»، اولویت medium، امروز). جزئیات بعداً از صفحهٔ تسک.
- پارس سادهٔ متن: «شیر بخر فردا» → تاریخ فردا؛ «!» در آخر → high. فقط این دو قاعده، نه بیشتر.
- Shortcut در manifest: لمس طولانی آیکون → «تسک جدید» و «لیست خرید».

### ۲.۶ نمای هفته
- تب «هفته»: ۷ ستون عمودی (موبایل: اسکرول افقی) یا لیست روزها با تعداد تسک هر روز. فقط خواندن + لمس روز → همان نمای «امروز» برای آن تاریخ (در MVP نوار تاریخ این را تا حدی دارد).

### ۲.۷ تقویم شمسی و Dark mode
- کتابخانهٔ `date-fns-jalali`؛ سوئیچ در پروفایل. ذخیره در دیتابیس همیشه میلادی (ISO)؛ فقط نمایش تغییر می‌کند.
- Dark mode با `next-themes` و توکن‌های Tailwind — شب‌ها زیاد استفاده می‌شود.

### معیار پذیرش فاز ۲
- [ ] نوتیفیکیشن روی گوشی هر دو نفر (iOS و Android) رسید.
- [ ] یک هفتهٔ کامل بدون هیچ تسک «گم‌شده» — هر تسک یا done یا skipped شده.
- [ ] لیست خرید در فروشگاه با یک دست قابل استفاده بود.
- [ ] اضافه کردن یک تسک زیر ۵ ثانیه.

---

## فاز ۳ — «زندگی خانه» (۳–۴ هفته)

### ۳.۱ زمان‌بندی‌های واقعی
- `schedule_type` جدید: `every_n_days` (هر ۳ روز گل آب دادن)، `monthly` (روز ۵ هر ماه: قسط، کرایه، قبض) — با گزینهٔ «اگر روز غیرکاری/جمعه بود، روز قبل».
- **قبض‌ها و پرداخت‌ها:** کتگوری «پرداخت» با فیلد `amount` اختیاری و تاریخ سررسید. یادآوری ۳ روز قبل. **نه** حسابداری کامل — فقط «یادم بیاور بپردازم».

### ۳.۲ چرخش کارها (Rotation)
- «هفتهٔ این من، هفتهٔ بعد تو»: `assignment_type='rotating'`, `rotation_members uuid[]`, `rotation_period ('daily','weekly')`. تابع `schedule.ts` مسئول امروز را حساب می‌کند.
- در UI فقط اسم مسئول امروز دیده می‌شود + «هفتهٔ بعد: نجیب».

### ۳.۳ روتین‌ها (Routines)
- گروهی از تسک‌ها که با هم فعال/غیرفعال می‌شوند: «روتین صبح»، «قبل از سفر»، «مهمان داریم».
- ادمین با یک لمس روتین را برای امروز فعال می‌کند → همهٔ تسک‌هایش به «امروز» می‌آیند.

```sql
create table routines (id uuid pk, household_id uuid, name text, icon text);
alter table tasks add column routine_id uuid references routines;
alter table tasks add column is_template boolean default false; -- تسک‌های داخل روتین فقط با فعال‌سازی ظاهر می‌شوند
```

### ۳.۴ درخواست تسک
- عضو نمی‌تواند برای ادمین تسک بگذارد، اما می‌تواند **درخواست** بدهد: «لطفاً لامپ آشپزخانه را عوض کن». ادمین می‌بیند، قبول/رد یا ویرایش می‌کند.
- `tasks.status in ('active','requested','rejected')`. درخواست‌ها در داشبورد ادمین با badge.
- جایگزین ساده‌تر: هر دو ادمین باشند. اگر در استفادهٔ واقعی نقش‌ها مشکل شد، همین را انتخاب کنید و ۳.۴ را نسازید.

### ۳.۵ یادداشت و عکس روی تسک
- بعد از تیک، اختیاری: یادداشت کوتاه یا عکس (Supabase Storage، فشرده‌شده در کلاینت با `browser-image-compression`، حداکثر ۵۰۰KB).
- کاربرد واقعی: «این را خریدم، همین بود؟»، «قبض را پرداخت کردم — رسید».

### ۳.۶ فید فعالیت
- تب «خانه»: لیست زمانی «فاطمه ✓ نان خرید — ۸:۱۰»، «نجیب تسک جدید اضافه کرد». فقط ۷ روز اخیر. Realtime.
- این جای بحث «تو انجام دادی؟» را می‌گیرد.

### ۳.۷ خلاصهٔ هفتگی
- پنجشنبه/جمعه شب یک نوتیفیکیشن + صفحه: «این هفته ۳۱ از ۳۵ تسک انجام شد. پرتکرارترین skip: ورزش.» بدون امتیاز و مقایسهٔ دو نفر — مقایسه در زوج‌ها دعوا می‌آورد.

### معیار پذیرش فاز ۳
- [ ] هیچ قبض/قسطی در دو ماه فراموش نشد.
- [ ] یک روتین («مهمان داریم») در موقعیت واقعی استفاده شد.
- [ ] فید فعالیت جای حداقل یک پیام واتساپ «انجام دادی؟» را گرفت.

---

## فاز ۴ — «پایداری و راحتی» (وقتی فازهای قبل جا افتادند)

### ۴.۱ آفلاین واقعی
- تیک زدن و اضافه کردن تسک بدون اینترنت → صف در IndexedDB (`idb-keyval`) → sync وقتی وصل شد. با Service Worker `Background Sync`. تعارض: آخرین تیک برنده.

### ۴.۲ اپ نیتیو با Capacitor
- اگر نوتیفیکیشن iOS ناپایدار بود، همان کد Next.js را با Capacitor بسته‌بندی کنید → پوش نیتیو (APNs/FCM)، ویجت، و آیکون در اپ‌استور خانگی (TestFlight/APK مستقیم کافی است، انتشار عمومی لازم نیست).

### ۴.۳ ورودی صوتی
- Web Speech API (fa-IR) در فیلد اضافهٔ سریع. برای وقتی دست بند است (آشپزی، رانندگی).

### ۴.۴ گسترش خانواده
- اضافه کردن اعضای بیشتر (بچه‌ها، والدین) با نقش `member` و **حالت ساده**: بدون کتگوری/اولویت، فقط چک‌لیست با آیکون بزرگ. برای بچه‌ها: ستاره به‌جای تیک.
- چند household برای یک کاربر (خانهٔ خود + خانهٔ والدین).

### ۴.۵ داده و امنیت
- Export CSV/JSON از پروفایل. پاک کردن حساب (GDPR-style).
- Rate limit روی Edge Functions. بک‌آپ روزانهٔ Supabase فعال.

---

## چیزهایی که **نباید** ساخته شود (مگر نیاز واقعی ثابت شود)
- امتیاز، لیدربورد، مقایسهٔ دو نفر — به رابطه آسیب می‌زند.
- چت داخل اپ — واتساپ دارید.
- ساب‌تسک‌های تودرتو، وابستگی تسک‌ها، Gantt — این خانه است نه پروژه.
- تقویم کامل ماهانه با drag & drop — روی موبایل بد کار می‌کند، نمای هفته کافی است.
- ادغام با Google Calendar — تا وقتی واقعاً هر روز لازم نشد.
- سیستم پرداخت/اشتراک — اپ شخصی است.

---

## ترتیب پیشنهادی ساخت (session به session در Claude Code)

| # | کار | فاز |
|---|---|---|
| 1 | تسک‌های عقب‌افتاده + status skipped | ۲.۲ |
| 2 | تسک مشترک | ۲.۳ |
| 3 | اضافهٔ سریع + manifest shortcuts | ۲.۵ |
| 4 | Web Push + خلاصهٔ صبح | ۲.۱ |
| 5 | لیست خرید ریل‌تایم | ۲.۴ |
| 6 | Dark mode + تقویم شمسی | ۲.۷ |
| 7 | نمای هفته | ۲.۶ |
| — | **دو هفته استفاده، بازخورد، حذف زوائد** | |
| 8 | every_n_days + monthly + قبض‌ها | ۳.۱ |
| 9 | چرخش کارها | ۳.۲ |
| 10 | روتین‌ها | ۳.۳ |
| 11 | یادداشت/عکس | ۳.۵ |
| 12 | فید فعالیت | ۳.۶ |
| 13 | خلاصهٔ هفتگی | ۳.۷ |
| 14 | درخواست تسک (یا هر دو ادمین) | ۳.۴ |
| — | **یک ماه استفاده** | |
| 15+ | فاز ۴ بر اساس نیاز | ۴ |

### پرامپت شروع فاز ۲
```
PLAN.md و PLAN-PHASE2.md را بخوان. کد فعلی را بررسی کن و مایگریشن‌های فاز ۲.۲
(تسک‌های عقب‌افتاده) را بنویس، بعد UI بخش «از دیروز مانده» را در نمای امروز اضافه
کن. قبل از تغییر schema، مایگریشن را به من نشان بده.
```
